{
  "name": "attendance-spa",
  "version": "1.0.0",
  "scripts": {
    "frontend": "cd frontend && yarn start",
    "api": "cd api && nodemon app.js",
    "dev": "concurrently --kill-others-on-fail \"yarn api\" \"yarn frontend\""
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  },
  "proxy": "http://localhost:3001"
}